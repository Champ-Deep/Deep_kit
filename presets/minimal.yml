# =============================================================================
# PRESET: Standard (formerly Minimal)
# =============================================================================
# The recommended starting point. AI chat interface + workflow automation.
#
# Included:
#   - Core (DEEPKIT_STORE, DEEPKIT_CACHE, DEEPKIT_ENGINE)
#   - DEEPKIT_CORE (command center - AI chat + generative UI)
#   - DEEPKIT_ORCHESTRATOR (workflow automation)
#
# Total: 5 services
# Ports: 5432, 6379, 11434, 7777, 5678
# RAM: ~5-6 GB
#
# Usage: ./install.sh --preset minimal
# =============================================================================

include:
  - path: ../docker-compose.yml
  - path: ../modules/deepkit-core.yml
  - path: ../modules/automation.yml
